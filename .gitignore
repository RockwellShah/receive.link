node_modules/
dist/
.wrangler/
.DS_Store
*.log
xcuserdata/
*.xcuserstate

# Secrets — NEVER commit. Server private keys + email creds live in Wrangler
# secrets (`wrangler secret put`), and local dev vars in .dev.vars.
.dev.vars
.env
keys/
*.private.jwk
*.secret
.gstack/

# Internal design docs — never ship in the public app repo.
/SPEC-*.md

# E2E inbox worker read token (local only, never committed)
e2e/inbox/.token

# Monetization test-env Worker config: untracked by design (env wiring for the
# mon worker, not part of the public app). Belt-and-suspenders so it can't be
# added by accident. Its secrets live in Wrangler, never in the file.
wrangler.mon.toml
