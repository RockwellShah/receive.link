# Files that live in web/ but must NOT publish to the CDN. NOTE: Cloudflare Pages IGNORES this file —
# it documents intent; the curated-dir deploy in the README is what actually enforces it.
*.ts
src/
fk/
core/
og-card-v2.html
tsconfig.json
