{
  "rules": [
    {
      "allowed": {
        "origins": ["https://drop-staging.filekey.app", "https://filekey-drop-staging.pages.dev"],
        "methods": ["GET", "PUT", "HEAD"],
        "headers": ["*"]
      },
      "exposeHeaders": ["ETag"],
      "maxAgeSeconds": 3600
    }
  ]
}
